DB_HOST=localhost
DB_PORT=3306
DB_USER=iceddaeng
DB_PASSWORD=iceddaeng
DB_NAME=iceddaeng
CORS_ORIGINS=http://localhost:5173
# 각자 발급해서 각자 넣습니다. 공유하지 않습니다.
#   1) https://aistudio.google.com/apikey 에서 Create API key (무료)
#   2) 아래에 따옴표 없이 붙여넣기
#   3) python scripts/check_gemini.py 로 확인
# 비워두면 문항은 기본 세트로, 리포트는 사전 문장으로 나갑니다 (기획안 §5).
# 게임 규칙과 능력치 계산은 키와 무관합니다.
GEMINI_API_KEY=
